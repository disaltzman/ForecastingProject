"0","```r
i_am(\"GOATMILK_1B.Rmd\")

df <- list.files(here(\"data\"),full.names = T,pattern = \"*.csv\") %>% map_df.(~fread(.))

# Clean vector names from Gorilla
df <- clean_names(df)

# Removed extraneous vectors
# Select only columns we want.
df %<>% 
  filter.(df$trial_number != \"BEGIN TASK\" | df$trial_number != \"END TASK\" ) %>%
  select.(participant_private_id,spreadsheet,trial_number,reaction_time,response_type,response,display,spreadsheet_fname,utc_date,spreadsheet_type,correct,spreadsheet_answer,spreadsheet_talker) %>% 
  filter.(display == \"LD\" | display == \"PC\") %>% 
  filter.(response_type == \"response\")
```"
